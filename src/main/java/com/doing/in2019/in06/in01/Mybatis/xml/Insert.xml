<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.uzone.payment.chain.chainpayrcv.dao.ChainEdgeVertexMapper">

	<!-- 直接插入 -->
	<insert id="insert" parameterType="com.sysndata.Test">
		insert into test 
			(
				id,
				create_time, 
				update_time,
				versions
			)
		values 
			(
				#{id,jdbcType=CHAR}, 
				#{createTime,jdbcType=TIMESTAMP},
				#{updateTime,jdbcType=TIMESTAMP},
				#{versions,jdbcType=INTEGER}
			)
	</insert>

	<!-- 选择插入 -->
	<insert id="insertSelective" parameterType="com.sysndata.Test">
		insert into test
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="id != null">
				id,
			</if>
			<if test="createTime != null">
				create_time,
			</if>
			<if test="updateTime != null">
				update_time,
			</if>
			<if test="versions != null">
				versions,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="id != null">
				#{id,jdbcType=CHAR},
			</if>
			<if test="createTime != null">
				#{createTime,jdbcType=TIMESTAMP},
			</if>
			<if test="updateTime != null">
				#{updateTime,jdbcType=TIMESTAMP},
			</if>
			<if test="versions != null">
				#{versions,jdbcType=INTEGER},
			</if>
		</trim>
	</insert>

</mapper>